{
  "name": "currency-phish-honeypot-frontend",
  "version": "1.0.0",
  "description": "Frontend dla aplikacji Currency-Phish-Honeypot",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
